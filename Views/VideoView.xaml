<UserControl x:Class="ExcursionTextbook.Views.VideoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf">

    <Grid Margin="32,24,32,32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding VideoTitle}" FontSize="24" FontWeight="Bold"
                   Foreground="{DynamicResource ChapterHeaderForeground}"
                   Margin="0,0,0,8"/>
        <Rectangle Grid.Row="1" Height="1" Fill="{DynamicResource BorderBrush}" Margin="0,0,0,20"/>

        <Border Grid.Row="2" CornerRadius="6" Background="Black"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                ClipToBounds="True">
            <wv2:WebView2 x:Name="VideoWebView"/>
        </Border>
    </Grid>
</UserControl>
