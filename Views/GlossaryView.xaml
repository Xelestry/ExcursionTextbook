<UserControl x:Class="ExcursionTextbook.Views.GlossaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:ExcursionTextbook.ViewModels">

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="32,24,32,32" MaxWidth="800">
                <TextBlock Text="Глоссарий" FontSize="24" FontWeight="Bold"
                           Foreground="{DynamicResource ChapterHeaderForeground}"
                           Margin="0,0,0,8"/>
                <TextBlock Text="Все термины в алфавитном порядке"
                           FontSize="13" Foreground="{DynamicResource NavForeground}"
                           Opacity="0.7" Margin="0,0,0,24"/>
                <Rectangle Height="1" Fill="{DynamicResource BorderBrush}" Margin="0,0,0,20"/>

                <ItemsControl ItemsSource="{Binding GlossaryGroups}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,0,0,16">
                                <!-- Letter header -->
                                <Border Background="{DynamicResource AccentBrush}"
                                        CornerRadius="4" Padding="10,4"
                                        HorizontalAlignment="Left" Margin="0,0,0,8">
                                    <TextBlock Text="{Binding Key}" FontSize="16" FontWeight="Bold"
                                               Foreground="White"/>
                                </Border>

                                <ItemsControl ItemsSource="{Binding}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Margin="0,0,0,6" Padding="14,10"
                                                    Background="{DynamicResource NavBackground}"
                                                    CornerRadius="6"
                                                    BorderBrush="{DynamicResource BorderBrush}"
                                                    BorderThickness="1">
                                                <StackPanel>
                                                    <TextBlock Text="{Binding Term}"
                                                               FontSize="14" FontWeight="Bold"
                                                               Foreground="{DynamicResource ChapterHeaderForeground}"
                                                               Margin="0,0,0,4"/>
                                                    <TextBlock Text="{Binding Definition}"
                                                               FontSize="13"
                                                               Foreground="{DynamicResource AppForeground}"
                                                               TextWrapping="Wrap"/>
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
